# Local RAG runtime data

Uploaded documents, Docling artifacts, ingestion jobs, and document manifests
are created here when running outside Docker. They may contain private source
content and are ignored by Git; only this explanation is versioned.

